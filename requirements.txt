PyYAML==6.0.2
tqdm==4.67.1
# YouTube's bot detection changes weekly, so keep yt-dlp current rather than pinning it.
# Large playlists (>100 items) also need the 2026-06-13 pagination fix, which is in nightly.
# Prefer the nightly channel:  python3 -m pip install -U --pre "yt-dlp[default]"
yt-dlp>=2026.6.13
# PO-token provider plugin (required to download most videos). See README.md.
bgutil-ytdlp-pot-provider
